---
description: Run the ShatrunZ application for a demo
---

1. Build the engine to ensure latest changes are applied
// turbo
cd engine && make

2. Start the server (uses ./start.sh to ensure venv)
// turbo
./start.sh

3. Open the application in the browser
// turbo
open http://localhost:8000
